Let's build the "Orbit" (CRM) module with a highly creative, Next-Gen, visually striking UI using Tailwind CSS. It should feel like a modern, high-speed sales command center.

1. Dynamic Page Header:

Title: "Orbit Command Center" (text-3xl font-extrabold tracking-tight text-gray-900).

Next to the title, add a pulsing "Live" indicator (a small green dot with animate-pulse and text "System Syncing").

Top Right: A sleek search bar (bg-white shadow-sm border-gray-100 rounded-full px-5 py-2.5 focus:ring-2 focus:ring-[#E31E24]/20) and a primary button "+ New Deal" (bg-gradient-to-r from-[#E31E24] to-red-600 text-white rounded-full px-6 py-2.5 shadow-lg shadow-red-500/30 hover:shadow-red-500/50 hover:-translate-y-0.5 transition-all).

2. Sales Velocity Metrics (High-Contrast Grid):

Create a 4-column grid (grid-cols-4 gap-6 mt-6).

Card 1 (Active Leads): White card. Value: 84. Add a subtle glowing blue background behind a 'Users' icon.

Card 2 (Urgent Follow-ups): Soft red background (bg-red-50). Value: 12 in heavy red text. Add a small 'Clock' icon that has a slight bounce on hover.

Card 3 (Win Rate): White card. Value: 24.5%. Instead of just text, add a CSS-based mini progress bar under the number filled to 25% in vibrant green.

Card 4 (Pipeline Value - The Hero Card): Dark mode card (bg-slate-900 text-white rounded-2xl p-6 shadow-xl relative overflow-hidden). Value: "₹ 45.2L" in large, bold text with a subtle background glow. Add a trending up arrow in emerald green.

3. "Smart Deal List" (Replacing the boring table):

Below the cards, add a container (bg-white rounded-2xl shadow-sm border border-gray-100 mt-8 overflow-hidden).

Header: "Priority Deals" with a "View All" ghost link on the right.

The List UI (Not a standard table): Create a vertical stack of 4 deal rows.

Row Styling: flex items-center justify-between p-5 border-b border-gray-50 hover:bg-gray-50/80 transition-all group.

Left Side (Company & Lead): A circular avatar with the Company's initials and a colorful gradient background. Next to it, the Lead Name (bold) and Company Name (grey, smaller).

Middle (The "Heat Bar"): Instead of text saying "Hot/Warm", create a visual "Temperature" bar. A small flex container with 3 segments. For a "Hot" lead, fill all 3 segments with red. For a "Warm" lead, fill 2 segments with orange.

Right Side (Value & Quick Actions): >    * Show the Deal Value (e.g., "₹ 2.5L" in bold text).

Quick Actions: Two circular icon buttons (Phone & Mail) that are normally transparent/grey but turn into vibrant solid colors (Green for phone, Red for mail) when the user hovers over the row (opacity-0 group-hover:opacity-100 transition-opacity).

Please generate this highly modern Orbit dashboard component.