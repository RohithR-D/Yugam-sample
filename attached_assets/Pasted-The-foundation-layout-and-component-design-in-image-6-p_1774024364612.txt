The foundation layout and component design in image_6.png and image_7.png are exactly the standard we are aiming for. Let's overhaul Yugam ERP's design language to match that high-end, clean SaaS aesthetic.

1. Refined Primary Layout (Sidebar & Header):

Use modern, clean sans-serif typography for all navigation items, matching the weight and spaciousness seen in image_6.png and image_7.png. The sidebar active state is good, keep the red left border, but ensure the red background fill is subtle (bg-red-50).

Replace Text Logo: Replace the text 'Yugam Logo' in the top-left sidebar with my uploaded logo image file: ED-ECS-Yugam.png. Style it to sit cleanly within the sidebar width.

Next-Gen Header Components: Create a refined header like in image_6.png & image_7.png. Add a sleek, compact search bar (bg-gray-50 border-gray-200 rounded-full px-4 py-2 text-sm w-64 focus:ring-1 focus:ring-red-200). Add circular icons for notifications (bell) and user profile (initials, e.g., "R", in a circular container with a gradient background).

2. The Redesigned Crew (HR Hub) Dashboard (Workspace Redesign):

Make the workspace (the main white container) look spacious and pure.

Metric Cards (Asthiraa Style): Completely redesign the metric cards for "Total Staff," "Present Today," "On Leave," and "Pending Approvals" to match image_7.png.

Each card should be pure white with a very light gray border (border-gray-100) and a subtle shadow.

The icon (Users, UserCheck, Calendar, Clock) must be enclosed within a circular border. The ring color should be complementary for neutral metrics and a strong Yugam Red ring (border-[#E31E24]) only for "Pending Approvals" to draw urgent attention.

Log Section (Entity Cards, Not a Table): Replace the standard data table for "Recent Attendance Logs" with a vertical list of detailed entity cards (inspired by image_6.png).

Each log entry card should feature:

A circular avatar with the employee's initials.

The Employee Name (bold) and Role.

A visual "Attendance Timeline" (replacing Punch In/Out columns). A linear bar with marked points for "Punch In" (e.g., 9:02 AM) and "Present" status and "Punch Out." For an absent employee, a simple visual indicator showing no activity on the timeline.

Compact icon-based actions (view eye icon, edit pencil icon) at the bottom.

Please update the entire layout and Crew module to reflect this new premium design system.