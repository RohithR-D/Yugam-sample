Let's build the "Flex" (Procurement) module, continuing our premium Asthiraa-inspired design system. When the user clicks "Flex (Procurement)" in the sidebar, the workspace should transition to the Procurement dashboard.

1. Page Header:

Title: "Flex Procurement" (text-2xl font-bold text-gray-900) with a subtext "Manage suppliers & purchase orders".

Top Right: Add a primary button "+ Create PO" (Solid yugam-red background, white text, rounded-md, with a shopping-cart icon).

2. Procurement Metric Cards (Asthiraa V2 Style):

Create a 4-column grid (gap-4 mt-6).

Cards: "Active POs" (blue icon), "Pending Deliveries" (orange icon), "Spend This Month" (green icon), and "Flagged Issues" (red icon).

Styling: Pure white cards, light gray borders, rounded-xl. Large bold values (e.g., "₹ 15.2L"). Small icons at the top left enclosed in a circular border matching their respective color.

3. Purchase Order List (With Visual Status Trackers):

Below the metrics, add a search bar: "Search POs or suppliers...".

Below the search, create a vertical stack of wide entity cards (flex flex-col gap-4 mt-6).

PO Row Styling (bg-white border border-gray-100 rounded-xl p-5 shadow-sm flex items-center justify-between hover:border-red-100 transition-all):

Left Section (PO Info): A subtle gray file icon. Next to it, the PO ID (e.g., "#PO-2026-112" in small gray) stacked above the Supplier Name (bold text, e.g., "Steel Matrix Ltd").

Middle Section 1 (Value): "Total Value" text stacked above the amount (e.g., "₹ 4.8L" bold).

Middle Section 2 (Visual Delivery Tracker): A custom linear tracker with 3 nodes: "Ordered", "Shipped", "Received". Connect them with a line. Color the nodes green as they are completed. This should look like the CRM pipeline but for logistics.

Right Section (Actions): Ghost icon buttons for "View Details" (Eye), "Download PO" (Download), and "Track Shipment" (Truck).

Create 4 dummy rows with varying statuses (Ordered, Shipped, Partially Received, Fully Received).

Please generate this Flex dashboard component and ensure the sidebar switches to it when "Flex" is clicked.