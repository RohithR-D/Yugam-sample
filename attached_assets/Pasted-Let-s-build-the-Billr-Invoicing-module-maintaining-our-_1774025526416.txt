Let's build the "Billr" (Invoicing) module, maintaining our premium Asthiraa-inspired design system. When the user clicks "Billr" in the sidebar, the workspace should transition to the Invoice Management dashboard.

1. Page Header:

Title: "Billr Invoicing" (text-2xl font-bold text-gray-900) with a subtext "Manage invoices & track payments".

Top Right: Add a primary button "+ Create Invoice" (Solid yugam-red background, white text, rounded-md, with a plus icon).

2. Financial Metric Cards (Asthiraa V2 Style):

Create a 4-column grid (gap-4 mt-6).

Cards: "Outstanding" (orange icon), "Overdue" (red icon), "Paid This Month" (green icon), and "Drafts" (gray icon).

Styling: Pure white cards, light gray borders, rounded-xl. Bold values (e.g., "₹ 8.5L"). Small icons at the top left enclosed in a circular border matching their respective color.

3. Invoice Tracking List (With Visual Payment Bars):

Below the metrics, add a full-width search bar: "Search invoices or clients...".

Below the search, create a vertical stack of wide cards (flex flex-col gap-4 mt-6).

Invoice Row Styling (bg-white border border-gray-100 rounded-xl p-5 shadow-sm flex items-center justify-between hover:border-red-100 transition-all):

Left Section (Identity): A subtle gray receipt icon. Next to it, Invoice ID (e.g., "#INV-2026-089" in small gray monospace) stacked above the Client Name (bold text).

Middle Section 1 (Value & Dates): Total Amount (e.g., "₹ 1.2L" bold) stacked above "Due: Apr 15, 2026" (small, gray).

Middle Section 2 (The Payment Progress Bar): A custom flex column (w-48). Top row shows a status pill (e.g., "Partial", "Paid", "Overdue"). Bottom row is a thin visual progress bar (h-1.5 rounded-full bg-gray-100). Fill the inner bar based on status (e.g., 50% yellow for Partial, 100% green for Paid, 0% red for Overdue).

Right Section (Actions): Three ghost icon buttons: Send Reminder (Bell), Download PDF (Download), and Share Payment Link (Link).

Create 4 dummy rows showcasing different statuses (Paid, Partial, Overdue, Sent).

Please generate this Billr dashboard component and ensure the sidebar switches to it when "Billr" is clicked.