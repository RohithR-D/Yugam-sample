Let's build the "Estimo" (Quotes) module, continuing our premium, spacious Asthiraa-inspired design system. When the user clicks "Estimo (Quotes)" in the sidebar, the main workspace should change to a Proposal Management dashboard.

1. Page Header:

Title: "Estimo Quotes" (text-2xl font-bold text-gray-900) with a subtext "Create & manage client proposals".

Top Right: Add a primary button "+ Create Quote" (Solid yugam-red background, white text, rounded-md, with a plus icon).

2. Metric Cards (Asthiraa V2 Style):

Create a 4-column grid (gap-4 mt-6).

Cards: "Drafts" (gray icon), "Sent" (blue icon), "Accepted" (green icon), and "Expired" (red icon).

Styling: Pure white cards, light gray borders, rounded-xl. Large bold values. Small icons at the top left enclosed in a circular border matching their respective color.

3. Quote Document List (Wide Card UI):

Below the metrics, add a search bar spanning the full width: "Search quotes or clients...".

Below the search, instead of a grid, create a vertical stack of wide cards (flex flex-col gap-4 mt-6) to represent a document list.

Quote Row Styling (bg-white border border-gray-100 rounded-xl p-4 shadow-sm flex items-center justify-between hover:border-red-100 transition-colors):

Left Section (Doc Info): A subtle gray document icon. Next to it, the Quote ID (e.g., "#EST-2026-041" in small gray monospace text) stacked above the Client Name (bold text).

Middle Section 1 (Value): "Total Value" text (small, gray) stacked above the Amount (e.g., "₹ 2.5L" bold).

Middle Section 2 (Dates): "Valid Until" text stacked above the date (e.g., "Mar 28, 2026").

Middle Section 3 (Status): A sleek status pill. Use standard colors (e.g., Green text/bg-green-50 for "Accepted", Blue for "Sent").

Right Section (Actions): Three ghost icon buttons: View (Eye), Download PDF (Download), and Send (Mail).

Create 4 dummy rows with varying statuses.

Please generate this Estimo dashboard component and ensure the sidebar switches to it when "Estimo" is clicked.