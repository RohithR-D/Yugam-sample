Let's make the "+ Mark Attendance" button interactive by creating a highly creative, modern, next-generation modal overlay. We want this to feel like a 2026 AI-powered tool, not a legacy ERP.

1. State Management & Smooth Transitions:

Add a React state to control the visibility of the modal (isAttendanceModalOpen).

Wire the "+ Mark Attendance" button to open this modal.

Add smooth enter/exit animations to the modal (e.g., scale up from 95% to 100%, fade in).

2. The Next-Gen Overlay (Glassmorphism):

Create a full-screen overlay using heavy blur: fixed inset-0 bg-slate-900/40 backdrop-blur-md z-50 flex items-center justify-center p-4 transition-all.

The modal card should be sleek: bg-white/95 backdrop-blur-xl rounded-2xl shadow-2xl border border-white/20 w-full max-w-lg overflow-hidden.

3. Modern Modal Header:

Give the header a subtle gradient background (e.g., very light grey to white) with padding p-6 border-b border-gray-100.

Title: "Log Attendance" in text-xl font-bold tracking-tight text-gray-900.

Add a small "AI Auto-detect" badge or a Sparkle icon next to the title in a soft purple/red gradient to imply smart features.

Add a sleek, circular close button (X icon) on the top right that spins slightly or highlights on hover.

4. Sleek Form Fields (The Content):

Inside a p-6 space-y-5 container, design the inputs to be modern: floating labels or subtle grey backgrounds (bg-gray-50) that turn white with a soft yugam-red focus ring (focus:ring-2 focus:ring-red-100 focus:border-red-500) when clicked.

Employee Select: A modern dropdown.

Date & Time Grid: A 3-column grid for Date, Punch In, and Punch Out.

Status: A dropdown for Present, Late, Half-Day.

Note Field: A text area with placeholder "Add manager notes or reason for override...".

5. The Action Footer:

p-6 bg-gray-50/50 border-t border-gray-100 flex justify-end gap-3.

Cancel Button: px-4 py-2 rounded-xl text-gray-600 hover:bg-gray-200 transition-colors.

Save Button: Make it pop. bg-[#E31E24] hover:bg-red-700 text-white px-6 py-2 rounded-xl font-medium shadow-lg shadow-red-500/30 hover:shadow-red-500/50 transition-all active:scale-95.

Please update the Crew Dashboard component to include this functioning, beautifully animated modal.