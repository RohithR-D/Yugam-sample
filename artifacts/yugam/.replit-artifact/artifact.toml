kind = "web"
previewPath = "/"
title = "Yugam ERP"
version = "1.0.0"
id = "artifacts/yugam"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25823

[services.development]
run = "pnpm --filter @workspace/yugam run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/yugam", "run", "build" ]
publicDir = "artifacts/yugam/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25823"
BASE_PATH = "/"
